<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maruani.games.mylittleheroesgi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.ShowInformationsActivity"></activity>
        <activity android:name=".views.ChooseWeaponActivity"></activity>
        <activity android:name=".views.CreatePlayerActivity"></activity>
        <activity android:name=".views.HomeActivity"></activity>
        <activity
            android:name=".views.MainActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>